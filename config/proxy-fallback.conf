# nginx-proxy's generated fallback returns 503 for unknown hosts. Make the
# intentional wildcard-DNS fallback a non-error 404 while retaining access logs.
server {
    listen 80 default_server;
    server_name "";
    server_tokens off;

    access_log /var/log/nginx/access.log fallback_json;
    return 404;
}
